{
  "name": "askbetter-root",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"server,client\" --prefix-colors \"yellow,cyan\" \"npm start --prefix server\" \"npm run dev --prefix askbetter\"",
    "install:all": "npm install --prefix server && npm install --prefix askbetter"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
